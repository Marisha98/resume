{{!--
Це код container файлу для summary сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

<div>
    {{#with main}}
    <main>
        <section>
            {{> title data="All skills" big=true}}
            <ul>
                {{#each skills}}
                <li>
                    {{name}}

                    {{#if point includeZero=true}} ({{point}}) {{/if}}
                    {{#if isTop}} (TOP) {{/if}}

                    {{#unless point includeZero=true}} (не вивчаємо) {{/unless}}

                    {{#unless point}}
                    {{#if point includeZero=true}}
                    (зараз вивчаємо)
                    {{/if}}
                    {{/unless}}
                </li>
                {{/each}}

                {{!-- <li>{{main.skills.[0]}}</li> - як варіант можна так, але писати порядковий номер строки в ручну
                <li>{{main.skills.[1]}}</li>
                <li>{{main.skills.[2]}}</li> --}}
            </ul>
        </section>

        <section>
            {{> title data="My hobbies" big=false}}
            <ul>
                {{#each hobbies}}
                <li>
                    {{name}}
                    {{#if isMain}} (favorite) {{/if}}
                </li>
                {{/each}}
            </ul>
        </section>
    </main>
    {{/with}}
</div>

{{!--
Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
Адже наш layout/default.hbs має вже структуру HTML
--}}